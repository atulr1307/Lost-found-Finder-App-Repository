# Lost-found-Finder-App-Repository
Lost and Found Finder is an Android app built with Kotlin, XML, and Android Studio to help users report and recover lost items. It uses Firebase for authentication and database storage, while Cloudinary handles item images. Users can post, search, and connect easily to return lost belongings efficiently.
